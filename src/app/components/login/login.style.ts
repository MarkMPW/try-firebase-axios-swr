import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
    paper: {
        padding: '24px',
    },
    fieldContainer: {
        display: 'flex',
        justifyContent: 'space-evenly',
    },
    button: {
        marginTop: '24px',
        padding: '25px',
    }
})

